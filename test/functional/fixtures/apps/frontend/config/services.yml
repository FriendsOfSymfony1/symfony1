all:
  parameters:
    my_app_param: foo

  services:
    my_app_service:
      class: sfOutputEscaperSafe
      arguments: [%my_project_param%]

    MyService:
      class: MyService


test:
  parameters:
    my_app_test_param: foo
